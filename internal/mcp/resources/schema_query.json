{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "query input",
  "description": "Input schema for the query tool.",
  "type": "object",
  "required": ["resource"],
  "properties": {
    "resource": {
      "type": "string",
      "description": "The resource to query.",
      "enum": ["sessions", "session", "messages", "status", "config", "ca_cert", "intercept_queue", "macros", "macro", "fuzz_jobs", "fuzz_results"]
    },
    "id": {
      "type": "string",
      "description": "Session ID or macro name. Required for session, messages, and macro resources."
    },
    "fuzz_id": {
      "type": "string",
      "description": "Fuzz job ID. Required for fuzz_results resource."
    },
    "filter": {
      "type": "object",
      "description": "Filter options for sessions, fuzz_jobs, and fuzz_results resources.",
      "properties": {
        "protocol": {
          "type": "string",
          "description": "Protocol filter (e.g. HTTP/1.x, HTTPS, WebSocket, HTTP/2, gRPC, TCP)."
        },
        "direction": {
          "type": "string",
          "description": "Message direction filter for messages resource (send or receive).",
          "enum": ["send", "receive"]
        },
        "method": {
          "type": "string",
          "description": "HTTP method filter (e.g. GET, POST)."
        },
        "url_pattern": {
          "type": "string",
          "description": "URL substring search pattern."
        },
        "status_code": {
          "type": "integer",
          "description": "HTTP response status code filter."
        },
        "body_contains": {
          "type": "string",
          "description": "Response body substring filter (fuzz_results)."
        },
        "status": {
          "type": "string",
          "description": "Fuzz job status filter (e.g. running, completed)."
        },
        "tag": {
          "type": "string",
          "description": "Fuzz job tag filter (exact match)."
        }
      },
      "additionalProperties": false
    },
    "fields": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of field names to include in the response (fuzz_jobs, fuzz_results)."
    },
    "sort_by": {
      "type": "string",
      "description": "Field to sort results by (fuzz_results: index_num, status_code, duration_ms, response_length)."
    },
    "limit": {
      "type": "integer",
      "description": "Maximum number of items to return (default 50, max 1000).",
      "minimum": 1,
      "maximum": 1000
    },
    "offset": {
      "type": "integer",
      "description": "Number of items to skip for pagination.",
      "minimum": 0
    }
  },
  "additionalProperties": false
}
