{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "resend input",
  "description": "Input schema for the resend tool.",
  "type": "object",
  "required": ["action", "params"],
  "properties": {
    "action": {
      "type": "string",
      "description": "The action to perform. 'replay' is a deprecated alias for 'resend'; 'replay_raw' is a deprecated alias for 'resend_raw'.",
      "enum": ["resend", "resend_raw", "tcp_replay", "compare", "replay", "replay_raw"]
    },
    "params": {
      "type": "object",
      "description": "Action-specific parameters.",
      "properties": {
        "flow_id": {
          "type": "string",
          "description": "Flow ID for resend, resend_raw, or tcp_replay."
        },
        "message_sequence": {
          "type": "integer",
          "description": "Message sequence number for WebSocket/streaming resend. Required for WebSocket flows."
        },
        "override_method": {
          "type": "string",
          "description": "HTTP method override for resend."
        },
        "override_url": {
          "type": "string",
          "description": "URL override for resend. Must include scheme and host."
        },
        "override_headers": {
          "type": "object",
          "description": "Header overrides for resend as key-value pairs (replaces matching headers).",
          "additionalProperties": { "type": "string" }
        },
        "add_headers": {
          "type": "object",
          "description": "Headers to add (appended to existing values for same key).",
          "additionalProperties": { "type": "string" }
        },
        "remove_headers": {
          "type": "array",
          "description": "Header names to remove.",
          "items": { "type": "string" }
        },
        "override_body": {
          "type": "string",
          "description": "Body override for resend (text)."
        },
        "override_body_base64": {
          "type": "string",
          "description": "Body override for resend (Base64-encoded binary)."
        },
        "body_patches": {
          "type": "array",
          "description": "Partial body modification rules.",
          "items": {
            "type": "object",
            "properties": {
              "json_path": { "type": "string" },
              "value": {},
              "regex": { "type": "string" },
              "replace": { "type": "string" },
              "encoding": { "type": "array", "items": { "type": "string" }, "description": "Codec chain to apply to the patch value before applying." }
            },
            "additionalProperties": false
          }
        },
        "override_host": {
          "type": "string",
          "description": "TCP connection target as host:port (independent of URL host). Subject to SSRF protection."
        },
        "follow_redirects": {
          "type": "boolean",
          "description": "Follow HTTP redirects automatically (default: false)."
        },
        "timeout_ms": {
          "type": "integer",
          "description": "Request timeout in milliseconds (default: 30000).",
          "minimum": 1
        },
        "dry_run": {
          "type": "boolean",
          "description": "Preview modified request without sending (default: false)."
        },
        "tag": {
          "type": "string",
          "description": "Tag to attach to the result flow."
        },
        "target_addr": {
          "type": "string",
          "description": "Target address (host:port) for resend_raw and tcp_replay."
        },
        "use_tls": {
          "type": "boolean",
          "description": "Use TLS for resend_raw or tcp_replay connection."
        },
        "override_raw_base64": {
          "type": "string",
          "description": "Full raw bytes replacement (Base64-encoded) for resend_raw."
        },
        "patches": {
          "type": "array",
          "description": "Byte-level patches for resend_raw.",
          "items": {
            "type": "object",
            "properties": {
              "offset": { "type": "integer" },
              "data_base64": { "type": "string" },
              "find": { "type": "string" },
              "find_base64": { "type": "string" },
              "replace": { "type": "string" },
              "replace_base64": { "type": "string" }
            },
            "additionalProperties": false
          }
        },
        "hooks": {
          "type": "object",
          "description": "Pre/post hooks for macro integration.",
          "properties": {
            "pre_send": { "type": "object" },
            "post_receive": { "type": "object" }
          },
          "additionalProperties": false
        },
        "flow_id_a": {
          "type": "string",
          "description": "First flow ID for compare action."
        },
        "flow_id_b": {
          "type": "string",
          "description": "Second flow ID for compare action."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
